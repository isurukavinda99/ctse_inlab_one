import 'package:flutter/cupertino.dart';

class PaddingBox extends StatelessWidget {
  const PaddingBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    );
  }
}
